# Python
__pycache__/
*.pyc

# uv / virtualenvs
.venv/
.viz-venv/

# Profiling artifacts (regenerable data)
*.prof
*.lprof
mprofile_*.dat
*.bin

# Downloaded JS/CSS assets for the interactive flame graph
.viz-assets/

# NOTE: generated HTML, JSON, and PNG/SVG reports are intentionally NOT ignored —
# they are documentation assets (inlined in / linked from chapter_2/README.md):
#   julia_profile.png, julia_flame.svg/.html, julia_flame_interactive.html,
#   mprof_*.png, scalene-julia.json/.html
